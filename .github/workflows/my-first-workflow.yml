name: My first forkflow
on: push

jobs:
  my-first-job:
    name: My first job
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v2
          
      - name: Compile
        run: |
          scripts/compile.sh
          
  my-second-job:
    name: My second job
    runs-on: ubuntu-latest

    steps:
      - name: Greeting
        run: |
          echo "Hello, world from second job"